Para criar a tabela no MySQL use:

1)  CREATE DATABASE fabricainteligente;

2)  CREATE TABLE maquinas (
    id INT UNSIGNED AUTO_INCREMENT,
    ip  VARCHAR(20) NOT NULL UNIQUE,
    Modelo  VARCHAR(255) NOT NULL,
    Fabricante  VARCHAR(255) NOT NULL,
    PRIMARY KEY(id, ip));

    CREATE TABLE dados (
    id INT UNSIGNED AUTO_INCREMENT,
    Valor  INT NOT NULL,
    Ip  VARCHAR(255) NOT NULL,
    PRIMARY KEY(id));